---
trigger: always_on
---

- The content API is used to store submitted content with related metadata and stored in `apps/content`
- The reader API is used to read and summarize submitted articles. It is stored in `apps/reader`